# .github/workflows/nih_reporter.yml
# Extracts grants from the NIH RePORTER API using the published Docker image.
name: NIH RePORTER Extraction

on:
  schedule:
    - cron: "30 6 * * *"
  workflow_dispatch:
    inputs:
      date_from:
        description: "Start date (YYYY-MM-DD). Default: 2025-03-01"
        required: false
        default: "2025-03-01"
      date_to:
        description: "End date (YYYY-MM-DD). Default: 2025-03-01"
        required: false
        default: "2025-03-01"

jobs:
  extract:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Create output directories
        run: mkdir -p output logs

      - name: Run NIH RePORTER extraction
        run: |
          FROM="${{ github.event.inputs.date_from || '2025-03-01' }}"
          TO="${{ github.event.inputs.date_to || '2025-03-01' }}"
          docker run --rm \
            -v ${{ github.workspace }}/output:/app/output \
            -v ${{ github.workspace }}/logs:/app/logs \
            ghcr.io/poornimaramakrishnan/grant-extract:latest \
            python -m grant_extract.scripts.nih_reporter \
              --date-from "$FROM" --date-to "$TO"

      - name: Upload .xlsx artifact
        uses: actions/upload-artifact@v4
        with:
          name: nih-reporter-grants
          path: output/*.xlsx
          retention-days: 30

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nih-reporter-logs
          path: logs/*.log
          retention-days: 7
